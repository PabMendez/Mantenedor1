<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Mantenedor{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('public/css/style.css') }}">
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <div class="container">
            <header>
                {% block seccion2 %}

                {% endblock %}
            </header>
            <nav>
                {% block seccion1 %}

                    <div>
                        <h4 >Menu para:</h4>
                        {{ render(controller('UsuarioBundle:Menu:list')) }}

                    </div>

                {% endblock %}
            </nav>
            <article>
                 <div class="mensajito">
                        {% for message in app.session.flashbag().get('mensaje') %}
                        <div>{{ message }}</div>
                        {%endfor%}
                    </div>
                {% block seccion3 %}
                   

                {% endblock %}
            </article>
        </div>
    </body>
</html>
